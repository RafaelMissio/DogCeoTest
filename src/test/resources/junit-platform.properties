# Habilita execução paralela
junit.jupiter.execution.parallel.enabled = true

# Define que testes e classes podem rodar em paralelo
junit.jupiter.execution.parallel.mode.default = concurrent
junit.jupiter.execution.parallel.mode.classes.default = concurrent

# Estratégia de paralelismo (dynamic = usa número de núcleos da máquina)
junit.jupiter.execution.parallel.config.strategy = dynamic

# (Opcional) Limitar número de threads
# junit.jupiter.execution.parallel.config.fixed.parallelism = 4

# (Opcional) Controlar paralelismo por anotação @Execution
# junit.jupiter.execution.parallel.mode = concurrent
